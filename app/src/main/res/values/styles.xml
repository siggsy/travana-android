<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorControlHighlight">@color/colorRipple</item>
        <item name="android:fontFamily">@font/google_sans</item>
        <item name="backgroundViewColor">@color/color_main_background</item>
        <item name="android:textColor">@color/color_text</item>
        <item name="containerBackground">@drawable/stretched_circle</item>
        <item name="containerBorderColor">@color/transparent</item>
        <item name="containerBorderMargin">3dp</item>
        <item name="containerBorderPadding">0dp</item>
        <item name="containerBorderPaddingSide">0dp</item>
        <item name="android:textColorHint">@color/text_hint</item>
        <item name="dividerColor">@color/light_gray</item>
        <item name="elevation">2dp</item>
        <item name="backgroundElevatedColor">@color/color_main_background_elevated</item>
        <item name="departureHourColor">@color/light_gray</item>
    </style>

    <style name="WhiteTheme" parent="MyTheme">
    </style>


    <style name="WhiteAlert" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/text_hint</item>
        <item name="android:textColorPrimary">@color/color_text</item>
        <item name="android:actionMenuTextColor">@color/color_text</item>
        <item name="textColorAlertDialogListItem">@color/color_text</item>
        <item name="android:background">@color/color_main_background</item>
        <item name="android:textColorSecondary">@color/color_text</item>
    </style>

    <style name="DarkAlert" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/text_hint</item>
        <item name="android:textColorPrimary">@color/color_text</item>
        <item name="android:actionMenuTextColor">@color/color_text</item>
        <item name="android:background">@color/color_main_background</item>
        <item name="textColorAlertDialogListItem">@color/color_text</item>
    </style>

    <style name="robotoBoldTitle">
        <item name="android:fontFamily">@font/roboto_bold</item>
        <item name="android:textSize">30sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Alert Dialog -->
    <style name="AlertDialog" parent="@style/ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <!-- Background Color-->
        <item name="android:background">@color/color_main_background</item>
        <!-- Text Color for title and message -->
        <item name="colorOnSurface">@color/color_black</item>
        <item name="android:textColor">@color/color_black</item>
        <!-- Style for positive button -->
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <!-- Style for negative button -->
        <item name="buttonBarNegativeButtonStyle">@style/PositiveButtonStyle</item>
    </style>

    <style name="PositiveButtonStyle" parent="@style/Widget.MaterialComponents.Button">
        <!-- text color for the button -->
        <item name="android:textColor">@color/colorAccent</item>
    </style>


    <!-- styles.xml -->
    <style name="AppTheme.Tabs.TextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
